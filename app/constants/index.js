const COMPASS = {
  NORTH: 0,
  EAST: 1,
  SOUTH: 2,
  WEST: 3,
};

const DIRECTIONS = {
  LEFT: "L",
  RIGHT: "R",
  MOVE: "M",
};

const COMPASS_ABBREVIATION = {
  N: COMPASS.NORTH,
  E: COMPASS.EAST,
  S: COMPASS.SOUTH,
  W: COMPASS.WEST,
};

const DIRECTIONS_ABBREVIATION = {
  L: DIRECTIONS.LEFT,
  R: DIRECTIONS.RIGHT,
  M: DIRECTIONS.MOVE,
};

module.exports = {
  COMPASS,
  DIRECTIONS,
  COMPASS_ABBREVIATION,
  DIRECTIONS_ABBREVIATION,
};
